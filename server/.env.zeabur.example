# Zeabur 部署环境变量

在 Zeabur 控制台中添加以下环境变量：

## 1. Supabase 配置
```
SUPABASE_URL=your_supabase_project_url
SUPABASE_ANON_KEY=your_supabase_anon_key
```

## 2. Node.js 环境
```
NODE_ENV=production
PORT=9091
```

---

**注意：**
- 将 `your_supabase_project_url` 替换为实际的 Supabase 项目 URL
- 将 `your_supabase_anon_key` 替换为实际的 Supabase anon key
- 这些值可以在您的 Supabase 项目设置中找到
